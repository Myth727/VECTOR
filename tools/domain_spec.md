# Domain Spec: VECTOR Harness Optimization

Fill this out before running meta_loop.py.
Template adapted from Meta-Harness ONBOARDING.md (Lee et al. 2026, arXiv:2603.28052).

---

## Domain Summary

**What are you trying to improve?**
[e.g. VECTOR coherence on long-form technical conversations with Claude Sonnet]

**Unit of evaluation:**
[e.g. one conversation session = one transcript JSON file, 10–30 turns]

**Fixed components (do not change):**
- Base model: claude-sonnet-4-6 (or your choice)
- VECTOR math stack: TF-IDF + JSD + Kalman + GARCH
- κ = 0.444 (Hudson Constant) — never adapted

**Allowed to change (harness search space):**
- Preset (DEFAULT, TECHNICAL, CREATIVE, RESEARCH, MEDICAL, CIRCUIT)
- Optional modules (EKF, Particle Filter, Lévy noise)
- Alt SDE model (CIR, Heston, Vasicek, SABR)

**Optimization budget:**
- Iterations: [e.g. 10]
- Transcripts in search set: [e.g. 20 sessions]
- Held-out test set: [e.g. 5 sessions — never exposed during search]
- Estimated API cost: [e.g. ~$2 at 1200 tokens/iteration]

---

## Harness and Search Plan

**Baseline preset:** DEFAULT

**Primary metric:** avg_c (average coherence score per session, higher = better)

**Secondary metrics:**
- drift_events (lower = better)
- health score (higher = better)
- H-signal count (lower = better for factual tasks)

**First search focus:**
[e.g. Try TECHNICAL vs RESEARCH vs DEFAULT on code review transcripts.
 If TECHNICAL wins, explore EKF and Particle Filter as mechanism changes.]

---

## Evaluation Plan

**Search set:** [path/to/your/transcripts/*.json] — N=[?] sessions
**Held-out test:** [path/to/held_out/*.json] — N=[?] sessions (NEVER use during search)

**Noise:** avg_c varies ±0.02 between runs. Delta > 0.03 = meaningful improvement.

**Leakage risk:** Low — harness does not see session content during config search,
only aggregate metrics.

**Runtime:** ~30s per candidate per transcript. 3 candidates × 20 transcripts = ~30 min/iteration.

---

## Experience and Logging

**Offline traces:** Export from VECTOR UI → EXPORT tab → JSONL Events
Transcript format is documented in `tools/tools_README.md`.

**Log structure:**
```
tools/logs/
  frontier_val.json         ← best config per preset
  evolution_summary.jsonl   ← one line per evaluated candidate
```

**Useful debugging:** Run `python tools/frontier.py --report` to see what's been tried.

---

## Open Questions and Unknowns

- Does EKF actually improve C-score on short sessions (< 10 turns)? unknown
- Does Particle Filter help for chaotic/creative sessions? unknown
- What's the right preset for mixed technical + conversational sessions? unknown
- κ sensitivity: does 0.444 outperform 0.500 on real transcripts? unknown — see CONTRIBUTING.md

---

*Fill in the bracketed fields before running meta_loop.py*
*Generated by VECTOR tools — adapted from Meta-Harness ONBOARDING.md*
